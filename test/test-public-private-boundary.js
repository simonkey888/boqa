'use strict';
const assert=require('assert');const fs=require('fs');const path=require('path');
const root=path.join(__dirname,'..');const html=fs.readFileSync(path.join(root,'dashboard/index.html'),'utf8');const app=fs.readFileSync(path.join(root,'dashboard/app.js'),'utf8');const worker=fs.readFileSync(path.join(root,'worker.js'),'utf8');const all=html+app;
assert(!all.includes('Centro de Cobros'));assert(!all.includes('/cobros'));assert(!/portfolio|bount|finanz|pago|ingreso/i.test(all));assert(!app.includes('/api/private/'));assert(!html.includes('fonts.googleapis'));assert(worker.includes("isPrivateBilling"));assert(worker.includes("error: 'not_found'"));assert(worker.includes('legacy demo implementation is unreachable'));assert(!fs.readFileSync(path.join(root,'.env.example'),'utf8').includes('BOQA_BILLING_PIN'));console.log('public/private boundary: PASS');
